<Page x:Class="SeaBattle.Views.Pages.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding WelcomePageViewModel, Source={StaticResource ViewModelLocator}}"
      Title="WelcomePage">

    <Grid>
        <StackPanel Background="White">
            <Button Command="{Binding LogoutCommand}" Content="Logout" />
            <StackPanel Orientation="Horizontal">
                <Label Content="Welcome, " />
                <Label Content="{Binding CurrentUser.Name}" />
                <Label Content="!" />
            </StackPanel>
            <Button Command="{Binding PlayCommand}" Content="Play!" />
        </StackPanel>

    </Grid>
</Page>
